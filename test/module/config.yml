# config files may contain app wide values …
defaultPictureFormat: jpg
# … or modul specific config values
mymodule:
  version: 0.1
  name: mymoduel
  author: me@home.com
  mountpoint: /
project:
  name: Example Project
  author: philipp.staender@gmail.com
  theme: 'minimal'
# all values will be merged to one big config file
# and are accessible via `modulware.getConfig()`